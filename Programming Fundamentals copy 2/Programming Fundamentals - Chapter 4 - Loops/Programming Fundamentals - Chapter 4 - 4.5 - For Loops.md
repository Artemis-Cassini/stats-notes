### Basics
- A **for loop** is a loop with three parts at the top: a loop variable initialization, a loop expression, and a loop variable update
- The statement i = i + 1 is so common that the language supports the shorthand **++i**, with ++ known as the **increment operator**. (Likewise, **—** is the **decrement operator**, -u means i = i -1)