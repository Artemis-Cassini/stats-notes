### Console Input with Prompts
- A **console** is a text-based interface for viewing program output and entering user input. One common approach for console-based programs is using prompts for each user input.
- A **prompt** is a message that indicates what the next expected user input is