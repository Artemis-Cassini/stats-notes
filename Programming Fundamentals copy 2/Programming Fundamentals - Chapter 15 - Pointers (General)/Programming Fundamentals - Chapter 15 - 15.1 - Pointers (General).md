### Pointers
- A **pointer** is a variable that contains a memory address
---
### Pointers and Dynamically Allocated Data
- A **dynamically allocated array** is an array whose size and memory location are determined during runtime
---
### Inserting Data in Arrays/Vectors vs Linked Lists
- A **linked list** consists of items that contain both data and a pointer (or link) to the next list item