**Multiple Distinct If Statements**
- Sometimes the programmer has multiple if statements in sequence, which looks similar to a multi-branch if-else statement but has a very different meaning
- Each if statement is independent and more than one branch can execute, in contrast to the multi-branch if-else arrangement 