**Switches**
- A switch controls whether electricity flows or not through a wire

**Processors and Memory**
- A processor can process a list of calculations, which are called instructions
- Memory is a circuit that can store 0s and 1s (binary)