- The concept of _inheritance_ is often confused with _composition_. Composition is the idea that one object may be made up of other objects. For instance, a "mother" class can be made up of objects like "name" (possibly a string object), "children" (which may be a list of Child objects), etc. Defining that "mother" class does _not_ involve inheritance, but rather just composing the sub-objects in the class.
