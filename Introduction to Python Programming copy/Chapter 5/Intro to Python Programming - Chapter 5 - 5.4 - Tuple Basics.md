**Tuples**
- A tuple, stores a collection of data, like a list, but is immutable, once it is created, the elements cannot be changed

**Named Tuples**
- A named tuple allows the programmer to define a new simple data type that consists of name attributes
- The named-tuple container must be imported to create a new named tuple