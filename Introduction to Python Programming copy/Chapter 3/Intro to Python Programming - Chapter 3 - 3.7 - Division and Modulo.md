**Division: Integer Rounding**
- The division operator / performs division and returns a floating-point number
- The floor division operator // can be used to round down the result of a floating_point division to the closest smaller whole number value

**Modulo (%)**
- The modulo operator evaluates the remainder of the division of two integer operands